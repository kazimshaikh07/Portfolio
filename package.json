{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio website with Django",
  "scripts": {
    "build": "bash ./vercel-build.sh || python build_static.py || node build.js"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "private": true
}